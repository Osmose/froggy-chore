select * from lists;
show tables;
select * from lists;
show tables;
select * from lists;
select * from lists;
select * from lists;
CREATE TABLE "migrations" (id   INTEGER PRIMARY KEY,name TEXT    NOT NULL,up   TEXT    NOT NULL,down TEXT    NOT NULL);
INSERT INTO migrations (id, name, up, down) VALUES (1, 'initial', 'CREATE TABLE lists (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  listId VARCHAR(255) NOT NULL UNIQUE,
  json TEXT NOT NULL
);', '');
.help
select * from lists;
.exit
